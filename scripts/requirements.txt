# Python dependencies for scripts/sync-board.py.
#
# Pinned by major version so security patches still apply automatically
# and the GitHub Actions pip cache key stays stable.

requests>=2.34.2,<3

# Pillow is used by sync-board.py to downscale + re-encode headshots
# uploaded via the Google Form. Optional at runtime — sync-board.py
# falls back to copying the raw bytes if Pillow is unavailable.
Pillow>=12.2.0,<13

# qrcode generates the QR matrix baked into the A4 poster by
# scripts/make-anthology-social.py. Matrix-only (no Pillow needed) — the
# script renders the modules as SVG rects. Optional at runtime: the script
# omits the QR (keeping the printed URL) if qrcode is unavailable.
qrcode>=8.2,<9
